# H1314/H943-C Reproducible Uniform-Pair Certificate

Classification: `h1314_h943c_uniform_pair_certificate_reproduced`

## Result

The complete reproducible certificate now reports

```text
R_r(t) <= 51/100,       r >= 5/2, t >= 0,
all_checks_pass=true.
```

Together with H1313, this gives the two exact H943-C budgets

```text
E[W^2 K]         <= 663/1250   < 1,
E[(W^2+2)W^2 K] <= 14229/5000 < 3.
```

The principal verifier is

```text
python tools/rh_h1314_h943c_uniform_pair_verifier.py
```

and its canonical output is

```text
research/riemann/h1314_h943c_uniform_pair_certificate.json.
```

## Exact Parameter Reconstruction

The parameter verifier reconstructs, rather than transcribes,

```text
V_n=P*T_n(r)-(9/4)r,
A=V_2,
B=V_3,
G=A*V_4-B^2,
s=A^3/B^2,
E[X^j]=A^(j-1)V_(j+2)/B^j.
```

On `5/2<=r<=3`, rational bounds prove

```text
153/4 < P=pi*exp(r) < 64.
```

The four required numerator polynomials have strictly positive full
bivariate Bernstein tensors on this rectangle:

| inequality | bidegree `(r,P)` | coefficients | exact minimum |
| --- | ---: | ---: | ---: |
| `s>=18` | `(6,3)` | 28 | `438438825/4096` |
| `delta2<=3/20` | `(6,2)` | 21 | `362076075/1024` |
| `delta3<=(7/2)delta2` | `(8,3)` | 36 | `1687066835625/16384` |
| `E[X^5]<=16/5` | `(15,5)` | 96 | `64926150610239650503125/33554432` |

For `r>=3`, `P>60` and the four shifted polynomials in
`X=r-3`, `Y=P-60` have nonnegative dense coefficients and strictly positive
nonzero coefficients. Their minimum nonzero coefficients are respectively

```text
1, 3, 1/2, 21
```

for

```text
s>=30,
delta2<=3/20,
delta3<=(7/2)delta2,
E[X^5]<=31/10.
```

The canonical parameter command and output are

```text
python tools/rh_h1314_h943c_parameter_polynomial_verifier.py
research/riemann/h1314_h943c_parameter_polynomial_certificate.json
```

The sibling file ending in `_certificate.py` is the exact polynomial engine.
Its direct command-line serializer is noncanonical because the first version
did not normalize two SymPy Boolean objects; the `_verifier.py` entry point
reruns all calculations and writes the valid JSON.

## Tilted Fifth Moment

The verifier reconstructs the exact Touchard identity

```text
((q+7)T_7(q)-T_8(q))/q
 =21q^5+280q^4+1050q^3+1204q^2+315q+6.
```

It also verifies the gamma-log correction and

```text
B-(r+3/2)A
 =r(4Pr-4P+18r+9)/8 > 0.
```

Thus `mu>=r+3/2`, `q=r exp(z/mu)<=r+1` on `0<=z<=1`, and

```text
d/dz log E[X^5 exp(zX)] <= 21/8.
```

Consequently

```text
E[X^5 exp(zX)] <= 4 exp(21z/8).
```

## Compact And Tail Regimes

For `0<=z<=1`, the exact parameter package gives

```text
R-1/2 <= U4(z)
 =61z^2/960+z^4 exp(21z/8)/180-(z/6)tanh(3z^3).
```

The separate exact five-interval verifier proves `U4(z)<1/100` using only
rational Taylor/geometric exponential bounds and the inequality
`tanh(u)>=u/sqrt(1+u^2)`. Its canonical output is

```text
research/riemann/h943c_u4_five_interval_certificate.json.
```

At `z=0`, the continuous limits are explicitly included:

```text
K_e -> 1/2, K_o -> 0, O -> 0, R -> 1/2.
```

For `z>=1`, H943-B and the certified parameters give

```text
R-1/2 <= (1/2)exp(H(z)),
H(1)<=-17/4,
H'(z)<=-(11/4)exp(z)<0.
```

The Arb enclosure regenerated by the main verifier is

```text
(1/2)exp(-17/4)
 =0.0071321169544996276366... < 1/100.
```

The compact interval, its endpoint, and the tail therefore cover all
`z>=0`, hence all `t>=0` because `z=a t` and `a>0`.

## Reproducibility Scope

Materialized here:

```text
the principal uniform-cap proof R<=51/100 and the H943-C budgets.
```

Not reconstructed from the available cards:

```text
the full shifted polynomial printed in the separate strong H946
corrected-pair branch proof R<=1/2+C_r t^2.
```

The cards record that polynomial's positivity but do not contain its complete
coefficient data. This certificate does not invent it. The uniform-cap route
already closes H943-C independently, while the strong H946 artifact remains
pending its own source-complete verifier.

This is a gamma-log theorem. It does not by itself prove an Xi transfer or the
Riemann Hypothesis.
